import React from "react";
import Preview from "../Preview";
import s from "./styles.module.css";
const Profile = ({ preview }) => {
  return <div className={s.profile}></div>;
};

export default Profile;
